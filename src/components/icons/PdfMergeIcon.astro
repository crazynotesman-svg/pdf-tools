---
/**
 * PdfMergeIcon — two documents being combined into one.
 * Extends PdfIconBase; `class` is forwarded for size/color control.
 */
import PdfIconBase from './PdfIconBase.astro';

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---
<PdfIconBase class={className}>
  <path d="m8 3 4 4-4 4" />
  <path d="M12 7h5a3 3 0 0 1 3 3v1" />
  <path d="m16 13 4 4-4 4" />
  <path d="M20 17h-5a3 3 0 0 1-3-3v-1" />
</PdfIconBase>
